#pragma once

const char preset[] = R"({
    "name": "clean limiter",
    "effects": ["hard_clip"],
    "params": [[0.15, 0, 0]],
    "description": "transparent hard clipping limiter; preserves tone while preventing clipping"
})";
