#pragma once

const char preset[] = R"({
    "name": "reverse delay",
    "effects": ["reversedelay"],
    "params": [[360, 0.2, 0.5]]
    "description": "reverse delay with a 360ms delay time, low feedback, and 50% mix"
})";
