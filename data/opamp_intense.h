#pragma once

const char preset[] = R"({
    "name": "intense opamp distortion",
    "effects": ["opamp"],
    "params": [[0.9, 0.7, 0.3, 0.8, 0.2]],
    "description": "aggressive opamp distortion with hard clipping emphasis; raw, cutting lead tone"
})";
