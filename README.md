# Guitar-Preset-AI
AI Model for creating presets for my Daisy Seed MultiFX Guitar Pedal

## Installing Dependencies

1. run `python -m venv .venv`

2. run `source venv/bib/activate`

3. run `pip install -r requirements.txt`
